# Replicate API token — powers the real virtual try-on (cuuupid/idm-vton).
# Create a free token at https://replicate.com/account/api-tokens
REPLICATE_API_TOKEN=r8_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Optional: per-session generations allowed per 24h (default 5).
GENERATION_DAILY_LIMIT=5
